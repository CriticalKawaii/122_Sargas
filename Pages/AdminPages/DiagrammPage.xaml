<Page x:Class="_122_Sargas.Pages.AdminPages.DiagrammPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:_122_Sargas.Pages.AdminPages"
      xmlns:charts="clr-namespace:System.Windows.Forms.DataVisualization.Charting;assembly=System.Windows.Forms.DataVisualization"
      mc:Ignorable="d"
      d:Background="#FF9ACBBA"
      d:DesignHeight="450"
      d:DesignWidth="800"
      Title="DiagrammPage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <StackPanel>
            <StackPanel Orientation="Horizontal"
                        HorizontalAlignment="Center">
                <TextBlock Text="Пользователь:"
                           Name="textBlockFIO"
                           VerticalAlignment="Center" />
                <ComboBox DisplayMemberPath="FIO"
                          x:Name="ComboBoxUser"
                          SelectionChanged="UpdateChart"
                          MinWidth="150" />
                <TextBlock Text="Тип диаграммы:"
                           Name="textBlockDiagramm"
                           VerticalAlignment="Center" />

                <ComboBox  SelectionChanged="UpdateChart"
                           x:Name="ComboBoxDiagram"
                           MinWidth="150" />
            </StackPanel>
            <StackPanel Orientation="Horizontal"
                        HorizontalAlignment="Center">
                <Button Content="Экспорт Word"
                        x:Name="ButtonWord" Click="ButtonWord_Click"/>
                <Button Content="Экспорт Excel"
                        x:Name="ButtonExcel" Click="ButtonExcel_Click"/>
            </StackPanel>
        </StackPanel>

        <WindowsFormsHost Grid.Row="1"
                          Margin="5"
                          HorizontalAlignment="Stretch"
                          VerticalAlignment="Stretch">
            <charts:Chart x:Name="ChartPayments">
                <charts:Chart.Legends>
                    <charts:Legend>

                    </charts:Legend>
                </charts:Chart.Legends>
            </charts:Chart>
        </WindowsFormsHost>


    </Grid>
</Page>
